<% @books.each do |book| %>
  <div class="book-card">
    <img class="card-img-top" src="https://freeiconshop.com/wp-content/uploads/edd/book-flat.png" alt="Card image cap">
    <div class="card-body">
      <h5 class="card-title"><%= book.title %></h5>
            <p><strong>Author:</strong> <%= book.author %></p>
            <p><strong>Genre:</strong> <%= book.genre %></p>
            <p><strong>Publish Date:</strong> <%= book.publish_date %></p>
    </div>
  </div>
<% end %>
